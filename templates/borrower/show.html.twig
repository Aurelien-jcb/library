{% extends 'base.html.twig' %}

{% block title %}Borrower{% endblock %}

{% block body %}
    <h1>Borrower</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ borrower.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ borrower.name }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ borrower.mail }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ borrower.phone }}</td>
            </tr>
            <tr>
                <th>Login</th>
                <td>{{ borrower.login }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ borrower.password }}</td>
            </tr>
            <tr>
                <th>Flag</th>
                <td>{{ borrower.flag ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Card</th>
                <td>{{ borrower.card ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('borrower_index') }}">back to list</a>

    <a href="{{ path('borrower_edit', {'id': borrower.id}) }}">edit</a>

    {{ include('borrower/_delete_form.html.twig') }}
{% endblock %}
